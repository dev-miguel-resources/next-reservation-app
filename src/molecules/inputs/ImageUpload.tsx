"use client";

import { ImageUploadProps } from "./interfaces/imageUploadProps.interface";

const ImageUpload: React.FC<ImageUploadProps> = ({ onChange, value }) => {
  // functions
  const handleUpload = () => {
    // pending
  };

  return <div>Hello am ImageUpload!</div>;
};
